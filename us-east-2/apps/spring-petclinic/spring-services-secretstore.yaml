kind: SecretStore
metadata:
  name: spring-petclinic-services-secretstore
  namespace: spring-petclinic
spec:
  provider:
    aws:
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets-sa
      region: us-east-2
      service: SecretsManager